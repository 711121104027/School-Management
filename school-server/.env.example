# Database
DB_HOST=your-db-host
DB_PORT=3306
DB_USER=your-db-user
DB_PASSWORD=your-db-password
DB_NAME=your-db-name

# Frontend URL for CORS (fill after frontend is deployed)
FRONTEND_ORIGIN=https://your-frontend-url

# Local dev only (do not commit .env with secrets)
FLASK_DEBUG=1
